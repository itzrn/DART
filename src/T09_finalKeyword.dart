void main(List<String> args) {
  /*
  var, dynamic data type variable acn be assigned with null value
  but int, double, bool, String null value can not be assigned to them
  */
  dynamic num;
  print(num);

  // here the value of amount gets finall, which will not get change at any 
  //place of the application
  final amount = 5;
  print(amount);
}
